node_modules/
dist/
/dist
*.tsbuildinfo
.vercel/

# Secrets. SUPABASE_SECRET_KEY acts as service_role and bypasses every
# row-level security policy in the project.
.env
.env.*
!.env.example

# Local development database. The deployed store is Supabase.
service/data/*.db
service/data/*.db-shm
service/data/*.db-wal
